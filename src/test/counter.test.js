// import chai from 'chai'; don't have to define this since chai is already in global scope
const assert = chai.assert;
// const {assert} = chai; another way you might see this

describe('counter controller', () => {
  it ('should run tests', () => {
    assert.ok(false);
  });
});
